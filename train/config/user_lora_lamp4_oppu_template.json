{
  "_doc": "User-LoRA Round 6 — OPPU recipe on LaMP-4 multi-user (K=100), cross-task replication of Round 5's LaMP-3 design. Stacked on top of A1-lamp ckpt-1000 (loaded via train.py's base_adapter plumbing). Fresh r=8 LoRA on q_proj+v_proj per OPPU's recipe (arXiv:2402.04401), trained on each user's snapshot-record profile (17-1100 entries in the top-100) with BM25 top-4 retrieval over the user's strictly-prior profile entries in the system slot at train time — matching what eval_lamp.py constructs at inference. max_seq_length=1024 is T3-pinned from data/lamp_user_stats/LaMP_4_round6_t3_sizing.json (round_up_to_256 of observed global max 969 across 9,325 examples; far below LaMP-3's 7168 pin — LaMP-4 article+retrieval prompts tokenize much shorter than LaMP-3's review-heavy ones). LR=1e-5 + weight_decay=1e-2 + 3 epochs is OPPU's published schedule, carried over verbatim from R5 (decision table item #6, no re-grill). Per_device_batch=2 + grad_accum=4 (effective_bs=8) is R5's final working config, carried over to skip the OOM iteration. Pinned design at experiments/2026-06-19-user-lora-round6-lamp4-multi-plan.md — do not modify without re-opening the design. Per-user JSON instantiations live at train/config/user_lora_lamp4_oppu_<user>.json, generated by data/lamp_user_stats/round6_gen_configs.py from this template (gitignored). Output dirs carry an explicit _oppu_ tag (train/checkpoints/user_lora_lamp4_<user>_oppu_seed0/) to disambiguate from the pre-existing R1/R2-B/R4 u00000011 checkpoints, since u00000011 lands at rank 1 in this K=100 pool.",

  "condition": "<<CONDITION>>",
  "seed": 0,
  "model_name_or_path": "data/models/SmolLM3-3B",
  "base_adapter": "train/checkpoints/a1_lamp_1ep_seed0/checkpoint-1000",
  "dataset_path": "<<DATASET_PATH>>",
  "output_dir": "<<OUTPUT_DIR>>",

  "lora": {
    "r": 8,
    "lora_alpha": 16,
    "lora_dropout": 0.05,
    "target_modules": ["q_proj", "v_proj"],
    "bias": "none",
    "task_type": "CAUSAL_LM"
  },

  "data": {
    "max_seq_length": 1024
  },

  "trainer": {
    "num_train_epochs": 3,
    "per_device_train_batch_size": 2,
    "gradient_accumulation_steps": 4,
    "learning_rate": 1e-5,
    "weight_decay": 0.01,
    "adam_beta1": 0.9,
    "adam_beta2": 0.999,
    "adam_epsilon": 1e-8,
    "max_grad_norm": 1.0,
    "lr_scheduler_type": "cosine",
    "warmup_ratio": 0.03,
    "bf16": true,
    "gradient_checkpointing": true,
    "gradient_checkpointing_kwargs": {"use_reentrant": false},
    "save_strategy": "epoch",
    "save_total_limit": 1,
    "logging_steps": 10,
    "optim": "adamw_torch",
    "report_to": "none",
    "remove_unused_columns": false,
    "dataloader_num_workers": 2
  },

  "wandb": {
    "_doc": "Disabled — report_to above is 'none'.",
    "project": "mobileFT_distill",
    "run_name": "<<CONDITION>>"
  }
}
